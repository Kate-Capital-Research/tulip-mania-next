# Tulip-Mania Environment Variables
# Copy this file to .env and fill in the actual values

# AI Service API Keys
# ---------------------

ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Ask Ignacio for your key. It gets generated in Anthropic's console.
# Note: This key is used for Anthropic's Claude AI service.

GEMINI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Ask Ignacio for your key. It gets generated in Google Cloud Console.
# Note: This key is used for Google's Gemini AI service. We limit the use to Generative AI and Vertex

# Financial Data Service API Keys (managed by tulip package)
# ----------------------------------------------------------

# These are typically configured in the tulip package, but documenting here for reference
FRED_API_KEY=your-fred-api-key-here
# Get your FRED API key from https://fred.stlouisfed.org/
# Note: This key is used for accessing FRED economic data.

BLOOMBERG_API_KEY=your-bloomberg-key-if-applicable
HAVER_API_KEY=your-haver-key-if-applicable

# JPMorgan Markets QIS Access (managed by tulip package)
JP_DATAQUERY_CLIENT_ID=your-jpmorgan-client-id
JP_DATAQUERY_CLIENT_SECRET=your-jpmorgan-client-secret

# System Environment Variables
TEMP=/tmp  # or appropriate temp directory path

# Data Paths (typically configured in tulip package)
# TULIP_DATA_PATH=/path/to/data
# TULIP_BRONZE_PATH=/path/to/bronze/data
# TULIP_SILVER_PATH=/path/to/silver/data

# Email Configuration (for internal reports)
# INTERNAL_REPORTS_EMAIL=reports@example.com

# PostgreSQL Database Configuration
PG_USERNAME=your-username
PG_PASSWORD=your-password
PG_HOSTNAME=your-hostname.amazonaws.com
PG_DB=your-database-name

# Optional: Proxy settings if behind corporate firewall
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1